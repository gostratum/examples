# Example environment variables for the order service
# Copy this file to .env and modify as needed

# Application environment (dev, staging, prod)
APP_ENV=dev

# Configuration file paths (comma-separated)
CONFIG_PATHS=./configs

# Database connection string
# DB_DSN=postgres://postgres:postgres@localhost:5432/orders?sslmode=disable

# HTTP server address
# HTTP_ADDR=:8080

# Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Disable go.work for this project
GOWORK=off